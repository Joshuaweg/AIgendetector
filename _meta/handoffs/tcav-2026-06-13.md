# CONTEXT HANDOFF
Generated: 2026-06-13

**Summary**: TCAV interpretability run completed but all 4 concepts failed validation due to a confirmed sc computation bug.

**Goal**: Understand what features the AI-generated video detector (93.53% accuracy) actually relies on, using TCAV concept probing across 4 human-defined concepts.

**Current state**: First full TCAV run is done. Results saved to `_meta/tcav/`. All 4 concepts failed — root cause diagnosed: `tcav_interpret.py` computes the TCAV score (sc) on the full fixed 50-video test set on every split, so sc never varies (0.480 ± 0.000 across all splits, all concepts). The bug is in the validation loop — sc needs to be recomputed per split on held-out probes, not the full test set.

**Branch**: N/A (not a git repo)

**Decisions made**:
- Used `python -u` flag to force unbuffered output through `tee` — resolved missing terminal output
- Identified sc computation as the primary bug (not probe quality or layer selection)
- Layer selection sc values do vary (0.0–0.45), confirming the bug is isolated to the validation loop

**Artifacts created**:
- `_meta/tcav_run.log` — full run output
- `_meta/tcav/tcav_results.json` — per-concept TCAV results
- `_meta/tcav/layer_selection.json` — layer scoring per concept

**Open threads**:
- [BLOCKER] `tcav_interpret.py` validation loop bug: sc is evaluated on full test set, not per-split held-out probes — fix before re-running
- [TODO] After fix, re-run and check if sc > 0.5 for any concept
- [TODO] If all sc still ≈ 0.5 after fix, probe quality or model feature alignment is the real issue
- [TODO] Probe imbalance worth addressing: `temporal_motion_inconsistency` is 98+/39− (skewed)

**TCAV results summary**:
| Concept | Best Layer | CAV Accuracy | sc | Status |
|---|---|---|---|---|
| temporal_motion_inconsistency | 11 | 0.938 | 0.480 | FAILED |
| texture_boundary_artifact | 3 | 0.870 | 0.480 | FAILED |
| lighting_shadow_decoupling | 11 | 0.730 | 0.480 | FAILED |
| frequency_domain_artifact | 3 | 0.778 | 0.480 | FAILED |

CAV accuracy is strong (model separates concept probes well). sc ≈ 0.5 means model decisions don't align with these concept directions — either the bug masks real signal, or the model learned different features.

**Next steps**:
1. Open `tcav_interpret.py` and fix the validation loop: sc must be computed on each split's held-out probe subset, not the global 50-video test set
2. Re-run: `python -u tcav_interpret.py --probes _meta/probes/ 2>&1 | tee _meta/tcav_run.log`
3. If sc still ≈ 0.5 after fix → run activation/saliency maps to discover what features the model actually uses
4. Consider rebalancing `temporal_motion_inconsistency` probes (currently 98+/39−)

**Warnings**:
- Do NOT re-run TCAV before fixing the sc bug — results will be identical and misleading
- The GPU (RTX 3060, 12GB) was at 32% util / 2.4GB VRAM during the run — headroom is fine

**Continuation prompt**:
> Continue TCAV interpretability work on AIgendetector. The first run (all 4 concepts failed) is done. Root cause: `tcav_interpret.py` validation loop computes sc on full test set instead of per-split probes — sc is frozen at 0.480 ± 0.000. Fix that bug first, then re-run. Read [[tcav-2026-06-13]] for full context.

---

## Related

**Superseded by:** [[per_token_tcav_2026_06_13]]

**Project:** [[MANIFEST]] · [[STATE]]

**Research:** [[integrated-gradients-baselines-research]] · [[sae-vision-transformer-research]]
